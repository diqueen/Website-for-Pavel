# 🚀 Инструкция по настройке и запуску проекта

## 📋 Предварительные требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** версии 18+ ([скачать](https://nodejs.org/))
- **npm** или **yarn** пакетный менеджер
- **Git** для работы с репозиторием
- **MongoDB** (локально или облачно)

## 🛠 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd marine-website
```

### 2. Установка зависимостей

```bash
# Установка зависимостей для всех частей проекта
npm run install:all
```

Или установка по отдельности:

```bash
# Frontend зависимости
cd frontend
npm install

# Backend зависимости
cd ../backend
npm install
```

### 3. Настройка переменных окружения

#### Frontend (.env)
```bash
cd frontend
cp env.example .env
```

Отредактируйте `frontend/.env`:
```env
# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000

# Google Analytics (опционально)
NEXT_PUBLIC_GA_ID=

# Google Maps API Key (для карты)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Contact Form Configuration
NEXT_PUBLIC_CONTACT_EMAIL=info@marine-company.ru
NEXT_PUBLIC_PHONE_NUMBER=+7 (999) 123-45-67

# Social Media Links
NEXT_PUBLIC_TELEGRAM_LINK=https://t.me/marine_company
NEXT_PUBLIC_WHATSAPP_LINK=https://wa.me/79991234567
```

#### Backend (.env)
```bash
cd backend
cp env.example .env
```

Отредактируйте `backend/.env`:
```env
# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/marine-website

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Admin Configuration
ADMIN_EMAIL=admin@marine-company.ru
ADMIN_PASSWORD=admin123
```

### 4. Настройка базы данных

#### Локальная MongoDB
```bash
# Установка MongoDB (Ubuntu/Debian)
sudo apt update
sudo apt install mongodb

# Запуск MongoDB
sudo systemctl start mongodb
sudo systemctl enable mongodb

# Проверка статуса
sudo systemctl status mongodb
```

#### Облачная MongoDB (MongoDB Atlas)
1. Зарегистрируйтесь на [MongoDB Atlas](https://www.mongodb.com/atlas)
2. Создайте новый кластер
3. Получите строку подключения
4. Обновите `MONGODB_URI` в `backend/.env`

### 5. Настройка Email (Gmail)

1. Включите двухфакторную аутентификацию в Google аккаунте
2. Создайте пароль приложения:
   - Перейдите в настройки безопасности Google
   - Выберите "Пароли приложений"
   - Создайте новый пароль для "Почта"
3. Используйте этот пароль в `EMAIL_PASS`

## 🚀 Запуск проекта

### Режим разработки

```bash
# Запуск всех сервисов одновременно
npm run dev
```

Или по отдельности:

```bash
# Terminal 1: Frontend
cd frontend
npm run dev

# Terminal 2: Backend
cd backend
npm run dev
```

### Режим продакшена

```bash
# Сборка проекта
npm run build

# Запуск
npm start
```

## 🌐 Доступ к приложению

После запуска приложение будет доступно по адресам:

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5000
- **API Health Check**: http://localhost:5000/api/health

## 📁 Структура проекта

```
marine-website/
├── frontend/                 # React/Next.js приложение
│   ├── src/
│   │   ├── app/             # Next.js App Router
│   │   ├── components/      # React компоненты
│   │   └── styles/          # Стили
│   ├── public/              # Статические файлы
│   └── package.json
├── backend/                  # Node.js/Express API
│   ├── src/
│   │   ├── routes/          # API маршруты
│   │   ├── middleware/      # Промежуточное ПО
│   │   └── utils/           # Утилиты
│   ├── uploads/             # Загруженные файлы
│   └── package.json
├── admin/                    # Админ-панель (будущее)
├── docs/                     # Документация
└── package.json             # Корневой package.json
```

## 🔧 Основные функции

### Frontend
- ✅ Современный дизайн в морской тематике
- ✅ Адаптивная верстка
- ✅ Анимации и интерактивность
- ✅ Каталог товаров с фильтрацией
- ✅ Галерея проектов
- ✅ Контактные формы
- ✅ SEO оптимизация

### Backend API
- ✅ RESTful API
- ✅ Валидация данных
- ✅ Обработка контактных форм
- ✅ Email уведомления
- ✅ Загрузка файлов
- ✅ Безопасность (CORS, Helmet, Rate Limiting)

### API Endpoints

#### Продукты
- `GET /api/products` - Список товаров
- `GET /api/products/:id` - Детали товара
- `GET /api/products/categories/list` - Категории
- `GET /api/products/search/:query` - Поиск

#### Услуги
- `GET /api/services` - Список услуг
- `GET /api/services/:slug` - Детали услуги
- `GET /api/services/categories/list` - Категории

#### Галерея
- `GET /api/gallery` - Проекты
- `GET /api/gallery/:id` - Детали проекта
- `GET /api/gallery/featured/list` - Избранные

#### Контакты
- `POST /api/contact/submit` - Отправка формы
- `POST /api/contact/quick` - Быстрая заявка
- `GET /api/contact/stats` - Статистика

#### Админка
- `GET /api/admin/dashboard/stats` - Статистика
- `GET /api/admin/contacts/recent` - Последние заявки
- `PUT /api/admin/contacts/:id/status` - Обновление статуса

## 🎨 Кастомизация

### Цветовая схема
Основные цвета определены в `frontend/tailwind.config.js`:

```javascript
colors: {
  marine: {
    50: '#f0f9ff',
    100: '#e0f2fe',
    // ... другие оттенки
  },
  ocean: {
    // ... оттенки океана
  }
}
```

### Шрифты
Используются Google Fonts:
- **Inter** - основной шрифт
- **Playfair Display** - заголовки

### Анимации
Анимации реализованы с помощью Framer Motion и CSS.

## 📱 Адаптивность

Сайт оптимизирован для:
- **Десктоп**: 1920px+
- **Планшеты**: 768px - 1024px
- **Мобильные**: 320px - 767px

## 🔒 Безопасность

### Frontend
- Валидация форм
- Защита от XSS
- HTTPS в продакшене

### Backend
- Helmet.js для заголовков безопасности
- CORS настройки
- Rate limiting
- Валидация входных данных
- Защита от SQL инъекций

## 📊 Мониторинг и аналитика

### Логирование
- Логи запросов (Morgan)
- Логи ошибок
- Логи производительности

### Аналитика (опционально)
- Google Analytics
- Яндекс.Метрика
- Пользовательские события

## 🚀 Деплой

### Frontend (Vercel)
```bash
cd frontend
vercel --prod
```

### Backend (Railway/Render)
```bash
cd backend
railway up
```

### Переменные окружения для продакшена
- Обновите все URL на продакшен версии
- Настройте SSL сертификаты
- Настройте CDN для статических файлов

## 🐛 Отладка

### Frontend
```bash
# Проверка линтера
cd frontend
npm run lint

# Проверка типов TypeScript
npm run type-check
```

### Backend
```bash
# Проверка синтаксиса
cd backend
node -c src/index.js

# Логи в реальном времени
tail -f logs/app.log
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки переменных окружения
4. Убедитесь, что порты не заняты другими приложениями

## 🔄 Обновления

Для обновления проекта:

```bash
git pull origin main
npm run install:all
npm run build
```

## 📝 Лицензия

MIT License - см. файл LICENSE для подробностей.

